# Intrusion-Detection-System
Building an intrusion detection system using the NSL-KDD dataset.
This has two files, one in which I have used logisic regression and another in which I have used a neural network model. 
